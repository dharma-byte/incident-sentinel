# Leave unset for local development: Vite proxies /api to http://127.0.0.1:8000
# (see vite.config.ts), so the browser sees a single origin and SSE needs no CORS.
#
# For a Vercel deployment, point this at the Render backend:
# VITE_API_URL=https://incident-sentinel-api.onrender.com
VITE_API_URL=
